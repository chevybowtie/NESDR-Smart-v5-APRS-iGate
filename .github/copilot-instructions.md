

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.14.0 -->
<!-- Updated: 2026-06-07T23:24:38.860Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
tests/test_setup_additional.py ← __future__, neo_core, neo_aprs, pytest
tests/test_setup_io.py ← __future__, neo_aprs, neo_core, pytest
tests/test_setup_validation.py ← __future__, neo_aprs, neo_core
tests/test_wspr_apply_ppm_writes_config.py ← neo_core, neo_wspr
tests/test_wspr_capture_publish.py ← neo_wspr
tests/test_wspr_cli_upload.py ← neo_wspr, neo_core, pytest
tests/test_wspr_decoder.py ← neo_wspr
tests/test_wspr_diagnostics.py ← neo_wspr
tests/test_wspr_json_output.py ← neo_core, neo_wspr
tests/test_wspr_persist_creates_backup.py ← neo_core, neo_wspr
src/neo_aprs/commands/setup.py ← __future__, getpass, neo_core, importlib
tests/test_wspr_calibrate.py ← unittest, neo_wspr, pytest
tests/test_wspr_capture.py ← neo_core, neo_wspr
tests/test_wspr_scan.py ← neo_wspr
tests/test_wspr_uploader.py ← neo_wspr, socketserver
src/neo_rx/cli.py ← __future__, neo_rx
tests/test_aprs_qconstruct.py ← __future__, neo_aprs
tests/test_aprsis_client.py ← __future__, neo_rx, pytest
tests/test_cli.py ← __future__, pytest, neo_rx
tests/test_listen_command_extended.py ← __future__, neo_aprs, neo_core, neo_rx, pytest
tests/test_term_keyboard.py ← neo_core
scripts/release.py ← importlib
src/neo_adsb/adsb/reporter.py ← __future__
src/neo_adsb/commands/diagnostics.py ← __future__, neo_adsb
src/neo_core/config.py ← __future__, tomli_w
src/neo_telemetry/mqtt_publisher.py ← __future__, ondisk_queue
src/neo_aprs/commands/diagnostics.py ← __future__, neo_core, importlib
src/neo_adsb/adsb/capture.py ← __future__
src/neo_adsb/adsb/diagnostics.py ← __future__
src/neo_adsb/commands/find_devices.py ← __future__
src/neo_adsb/commands/listen.py ← __future__, neo_core
src/neo_adsb/commands/setup.py ← __future__, neo_core
src/neo_aprs/commands/listen.py ← __future__, neo_core, neo_aprs
src/neo_core/cli.py ← neo_rx
src/neo_core/term.py ← __future__
src/neo_rx/radio/nesdr.py ← __future__, base, importlib
src/neo_wspr/commands/listen.py ← __future__, neo_core
tests/test_adsb_capture.py ← neo_adsb
tests/test_adsb_cli.py ← unittest, neo_core, pytest
tests/test_adsb_diagnostics.py ← neo_adsb
tests/test_adsb_reporter.py ← neo_adsb
```

## changes (last 5 commits — 8 weeks ago)
```
src/neo_adsb/adsb/capture.py                  ~Dump1090Client  ~AdsbCapture
src/neo_adsb/adsb/diagnostics.py              +check_dump1090_json  +_check_lsusb_for_rtl  +check_readsb_config  ~check_dump1090_json
src/neo_adsb/commands/find_devices.py         +run_find_devices_cmd
src/neo_adsb/commands/listen.py               +restore_level  ~run_listen  ~_display_aircraft
src/neo_adsb/commands/setup.py                ~run_setup
src/neo_aprs/commands/listen.py               +_read_system_uptime_seconds  +_format_duration  ~run_listen  ~_append_q_construct
src/neo_core/cli.py                           ~build_parser  ~main
src/neo_core/term.py                          +process_commands  ~process_commands  ~drain_command_queue
src/neo_wspr/commands/listen.py               ~run_listen
tests/test_adsb_capture.py                    ~TestDump1090Client
tests/test_adsb_cli.py                        ~TestAdsbCli
tests/test_adsb_diagnostics.py                ~TestDump1090JsonCheck
tests/test_adsb_reporter.py                   ~TestAdsbExchangeReporter
```

## docs

### docs/developer.md
```
h1 Install packages in dependency order
h1 from the repo root
h1 install only the wheels built from this repo
h1 install any runtime deps reported on import
h1 quick import checks
h1 CLI smoke
h1 Dry-run for version 0.2.8
h1 Build and create tags locally (no upload)
h1 Build, create tags, and upload to PyPI (requires credentials)
code-fence bash
code-fence plain
```

### docs/release_guide.md
```
h1 Release Guide
h2 Overview
h2 Automatic Release (Recommended)
h3 1. Prepare release branch
h1 Ensure tree is clean
h1 Create release branch
h3 2. Update CHANGELOG.md
h2 [Unreleased]
h3 Added
h3 Changed
h3 Fixed
h3 Removed
h3 3. Verify the release
h3 4. Dry-run the release
h3 5. Execute the release
h3 6. Push release branch and tags
h1 Push the release branch
h1 Push all tags
h3 7. Create pull request on GitHub
h3 8. Publish to PyPI (optional)
h1 Switch to master and pull merged changes
h1 Upload to PyPI (requires credentials)
h2 Manual Release (Step-by-Step)
h1 Ensure working directory is clean
h1 Create release branch from develop
```

### docs/INSTALL.md
```
h1 Installation (Debian 13)
h1 or curl -fsSL https://bit.ly/49l7lnD | bash
code-fence bash
code-fence plain
code-fence ini
```

### docs/diagnostics.md
```
h1 Diagnostics Command Outline
h2 Command Summary
h2 Device Discovery
h2 Checks Performed (Common)
h3 1. Environment
h3 2. Configuration Validation
h3 3. SDR Hardware
h2 ADS-B-Specific Checks
h3 4. Decoder (readsb/dump1090)
h3 5. RTL-SDR Availability
h3 6. ADS-B Exchange Integration
h2 APRS-Specific Checks
h3 7. Direwolf / KISS
h3 8. APRS-IS Uplink
h2 WSPR-Specific Checks
h3 7. Decoder Binary
h3 8. Upconverter Detection
h2 Configuration & Paths (All Modes)
h3 9. Configuration & Paths
h2 Output Schema
h2 Exit Codes
h2 Implementation Notes
h2 Log retention
code-fence bash
code-fence plain
```

### docs/direwolf-setup-debian.md
```
h1 Direwolf Installation and Setup (Debian)
h2 1. Install Packages
h2 2. Verify Installation
h2 3. Prepare Config Directory
h2 4. Render Direwolf Configuration
h2 5. Run Direwolf with NESDR Audio
h2 6. Enable KISS Port Access
h2 7. Test with Diagnostics
h2 8. Optional: Autostart Service
code-fence plain
```

### docs/troubleshooting.md
```
h2 RTL-SDR Thermal Issues
h1 Before (thermal issues)
h1 After (stable)
h2 General Troubleshooting Tips
code-fence bash
code-fence plain
```

## scripts

### scripts/release.py
```
def run_command(cmd: list[str], cwd: Path | None, capture: bool) → str
def find_project_root() → Path
def check_git_clean(root: Path) → None
def get_current_version(root: Path) → str
def validate_version_sync(root: Path, expected: str) → None
def update_changelog(root: Path, version: str) → None
def tag_exists(root: Path, tag: str) → bool
def create_git_tags(root: Path, version: str, dry_run: bool, force: bool) → None
def build_packages(root: Path, dry_run: bool) → None
def upload_to_pypi(root: Path, dry_run: bool) → None
def commit_changes(root: Path, version: str, dry_run: bool) → None
def main() → None
```

### scripts/sync_versions.py
```
def find_project_root() → Path
def get_current_versions(root: Path) → dict[str, str]
def update_version_in_file(path: Path, new_version: str) → None
def sync_versions(root: Path, new_version: str) → None
def show_versions(root: Path) → None
def validate_version(version: str) → bool
def main() → None
```

### scripts/verify_release.sh
```
# Multi-package verification script for neo-rx suite.
```

### scripts/run_direwolf.sh
```
# Launch Direwolf using rtl_fm as the audio source.
function usage()
```

## src

### src/neo_aprs/commands/setup.py
```
class _Prompt
  def optional_string(label: str, default: object | None) → str | None
  def optional_float(label: str, default: object | None) → float | None
  def secret(label: str, default: object | None) → str
class _PromptSession
  def ask_yes_no(message: str, default: bool) → bool
def run_setup(args: Namespace) → int
def prompt_yes_no(message: str, *, default: bool, input_func: Callable[[str], str] | None, echo: Callable[[str], None] | None) → bool
```

### src/neo_rx/cli.py
```
def build_parser() → argparse.ArgumentParser
def main(argv: list[str] | None) → int
```

### src/neo_adsb/adsb/reporter.py
```
@dataclass AdsbExchangeConfig(username, latitude, longitude, altitude_m, input_host, input_port, uuid?, mlat_enabled, privacy_enabled)
@dataclass AdsbExchangeStatus(feed_service_active, mlat_service_active, feed_connected, mlat_connected, uuid?, username?, last_check?)
class AdsbExchangeReporter
  def config() → AdsbExchangeConfig | None
  def is_installed() → bool
  def get_uuid() → str | None
  def get_status() → AdsbExchangeStatus
  def load_config() → AdsbExchangeConfig | None
  def save_config(config: AdsbExchangeConfig | None) → bool
  def restart_services() → bool
  def get_feed_check_urls() → dict[str, str]
```

### src/neo_adsb/commands/diagnostics.py
```
def run_diagnostics_cmd(args: Namespace) → int
```

### src/neo_core/config.py
```
@dataclass StationConfig(callsign, passcode, passcode_in_keyring, aprs_server, aprs_port, latitude?, longitude?, altitude_m?, beacon_comment?, software_tocall?, kiss_host, kiss_port, center_frequency_hz, sample_rate_sps, gain?, ppm_correction?, wspr_enabled, wspr_auto_upload, wspr_bands_hz?, wspr_capture_duration_s, wspr_grid?, wspr_power_dbm, wspr_uploader_enabled, upconverter_enabled, upconverter_lo_offset_hz, mqtt_enabled, mqtt_host?, mqtt_port?, mqtt_topic?)
def get_config_dir() → Path
def get_data_dir() → Path
def get_mode_data_dir(mode: str | None) → Path
def get_logs_dir(mode: str | None) → Path
def get_wspr_runs_dir(run_label: str | None) → Path
def resolve_config_path(path: str | Path | None) → Path
def load_config(path: str | Path | None) → StationConfig
def save_config(config: StationConfig, path: str | Path | None) → Path
def config_summary(config: StationConfig) → str
def keyring_supported() → bool
def store_passcode_in_keyring(callsign: str, passcode: str) → None
def delete_passcode_from_keyring(callsign: str) → None
```

### src/neo_telemetry/mqtt_publisher.py
```
class MqttPublisher
  def connect() → None
  def publish(topic: str, payload: dict) → None
  def close() → None
```

### src/neo_aprs/commands/diagnostics.py
```
@dataclass Section(name, status, message, details)
def run_diagnostics(args: Namespace) → int
```

### src/neo_adsb/adsb/capture.py
```
@dataclass AircraftState(hex_id, flight?, altitude_ft?, ground_speed_kt?, track_deg?, latitude?, longitude?, vertical_rate_fpm?, squawk?, rssi_db?, seen_s, seen_pos_s?, messages, category?, first_seen, last_seen)
@dataclass CaptureStats(start_time, total_messages, total_aircraft, unique_aircraft, max_range_nm, max_altitude_ft)
class Dump1090Client
  def poll() → list[AircraftState]
  def get_aircraft(hex_id: str) → AircraftState | None
  def get_all_aircraft() → list[AircraftState]
  def get_stats() → CaptureStats
  def clear_stale(max_age_s: float) → int
class AdsbCapture
  def add_callback(callback: Callable[[list[AircraftState]], None]) → None
  def start() → None
  def stop() → None
  def is_running() → bool
  def get_client() → Dump1090Client
```

### src/neo_adsb/adsb/diagnostics.py
```
@dataclass DiagnosticResult(name, status, message, details)
@dataclass DiagnosticsReport(timestamp, checks)
def check_dump1090_installed() → DiagnosticResult
def check_dump1090_running() → DiagnosticResult
def check_dump1090_json(json_path: str | Path) → DiagnosticResult
def check_rtl_sdr() → DiagnosticResult
def check_adsbexchange_installed() → DiagnosticResult
def check_adsbexchange_services() → DiagnosticResult
def check_readsb_config() → DiagnosticResult
def run_diagnostics(check_adsbexchange: bool, json_path: str | Path | None) → DiagnosticsReport
```

### src/neo_adsb/commands/find_devices.py
```
def run_find_devices_cmd(args: Namespace) → int
```

### src/neo_adsb/commands/listen.py
```
def run_listen(args: Namespace) → int
```

### src/neo_adsb/commands/setup.py
```
def run_setup(args: Namespace) → int
```

### src/neo_adsb/pyproject.toml
```
table [build-system]
table [project]
table [project.urls]
table [project.optional-dependencies]
table [tool.setuptools]
table [tool.pytest.ini_options]
table [tool.ruff]
key build-backend
key name
key version
key description
key authors
key requires-python
key license
key dependencies
key Homepage
key addopts
key asyncio_mode
key line-length
```

### src/neo_aprs/commands/listen.py
```
@dataclass _StationActivity(count, last)
def run_listen(args: Namespace) → int
```

### src/neo_aprs/pyproject.toml
```
table [build-system]
table [project]
table [project.urls]
table [project.optional-dependencies]
table [tool.setuptools]
table [tool.pytest.ini_options]
table [tool.ruff]
key build-backend
key name
key version
key description
key authors
key requires-python
key license
key dependencies
key Homepage
key addopts
key asyncio_mode
key line-length
```

### src/neo_core/cli.py
```
def build_parser() → argparse.ArgumentParser
def main(argv: list[str] | None) → int
```

### src/neo_core/pyproject.toml
```
table [build-system]
table [project]
table [project.urls]
table [project.optional-dependencies]
table [tool.setuptools]
table [tool.setuptools.package-data]
table [tool.pytest.ini_options]
table [tool.ruff]
key build-backend
key name
key version
key description
key authors
key requires-python
key license
key dependencies
key Homepage
key addopts
key asyncio_mode
key line-length
```

### src/neo_core/term.py
```
def supports_color() → bool
def status_label(level: ColorLevel, *, enabled: bool) → str
def color_text(text: str, *, color: str, enabled: bool) → str
def start_keyboard_listener(stop_event: threading.Event, command_queue: Queue[str], *, name: str) → threading.Thread | None
def drain_command_queue(queue: Queue[str]) → list[str]
def process_commands(queue: Queue[str], handlers: dict[str, "callable"], *, default: "callable" | None) → None
```

### src/neo_rx/radio/nesdr.py
```
class NESDRBackend(RadioBackend)
  def __init__(device_index: int) → None
  def open() → None
  def configure(settings: RadioSettings) → None
  def read_samples(num_samples: int) → Iterable[complex]
  def get_status() → RadioStatus
  def close() → None
```

### src/neo_telemetry/pyproject.toml
```
table [build-system]
table [project]
table [project.urls]
table [project.optional-dependencies]
table [tool.setuptools]
table [tool.pytest.ini_options]
table [tool.ruff]
key build-backend
key name
key version
key description
key authors
key requires-python
key license
key dependencies
key Homepage
key addopts
key asyncio_mode
key line-length
```

### src/neo_wspr/commands/listen.py
```
def run_listen(args: Namespace) → int
```

### src/neo_wspr/pyproject.toml
```
table [build-system]
table [project]
table [project.urls]
table [project.optional-dependencies]
table [tool.setuptools]
table [tool.setuptools.package-data]
table [tool.pytest.ini_options]
table [tool.ruff]
key build-backend
key name
key version
key description
key authors
key requires-python
key license
key dependencies
key Homepage
key addopts
key asyncio_mode
key line-length
```

## tests

### tests/test_setup_additional.py
```
def test_run_setup_reset_removes_config(tmp_path: Path, monkeypatch, caplog) → None
def test_interactive_prompt_keyring_store(monkeypatch) → None
def test_interactive_prompt_keyring_missing_backend(monkeypatch, caplog) → None
def test_prompt_yes_no_with_invalid_response(monkeypatch, caplog) → None
def test_maybe_render_direwolf_config_template_missing(tmp_path: Path, monkeypatch, caplog) → None
def test_maybe_render_direwolf_config_existing_decline(tmp_path: Path, monkeypatch, caplog) → None
def test_run_hardware_validation_reports(monkeypatch, caplog) → None
def test_run_hardware_validation_launches_probe(monkeypatch, caplog) → None
```

### tests/test_setup_io.py
```
def test_prompt_string_validates_and_transforms() → None
def test_prompt_optional_string_returns_default() → None
def test_prompt_integer_enforces_bounds() → None
def test_prompt_optional_float_handles_invalid() → None
def test_prompt_secret_handles_defaults_and_mismatch() → None
def test_prompt_session_ask_yes_no_uses_injected_functions() → None
def test_prompt_yes_no_invalid_then_default(monkeypatch, capsys) → None
def test_format_prompt_and_parse_helpers() → None
```

### tests/test_setup_validation.py
```
class _FakeRtlProcess
  def __init__() → None
  def terminate() → None
  def wait(timeout: float | None) → int
  def kill() → None
class _FakeDirewolfProcess
  def __init__() → None
  def wait(timeout: float | None) → int
  def terminate() → None
  def kill() → None
def test_extract_ppm_from_output_detects_value() → None
def test_extract_ppm_from_output_none() → None
def test_tail_file_returns_last_lines(tmp_path: Path) → None
def test_report_direwolf_log_summary_missing(tmp_path: Path, monkeypatch, caplog) → None
def test_report_direwolf_log_summary_includes_recent_lines(tmp_path: Path, monkeypatch, caplog) → None
def test_can_launch_direwolf_true(monkeypatch) → None
def test_can_launch_direwolf_false(monkeypatch) → None
def test_launch_direwolf_probe_missing_config(tmp_path: Path, monkeypatch, caplog) → None
def test_launch_direwolf_probe_success(tmp_path: Path, monkeypatch, caplog) → None
def test_launch_direwolf_probe_direwolf_timeout(tmp_path: Path, monkeypatch, caplog) → None
```

### tests/test_wspr_apply_ppm_writes_config.py
```
def make_sample_config(tmp_path: Path) → Path
def test_apply_ppm_writes_config(tmp_path)
```

### tests/test_wspr_capture_publish.py
```
class MockPublisher
  def __init__()
  def connect()
  def publish(topic, payload)
  def close()
def fake_capture_fn(band_hz: int, duration_s: int)
def test_capture_publishes(tmp_path: Path)
```

### tests/test_wspr_cli_upload.py
```
def test_upload_requires_configuration(monkeypatch, caplog, base_args)
def test_upload_requires_uploader_enabled(monkeypatch, caplog, tmp_path, base_args)
```

### tests/test_wspr_decoder.py
```
def test_parse_wsprd_fixture()
```

### tests/test_wspr_diagnostics.py
```
def test_detect_upconverter_hint_from_spots()
def test_upconverter_hint_empty_spots()
def test_upconverter_hint_no_offset()
def test_upconverter_hint_large_offset()
def test_upconverter_hint_snr_low_boosts_confidence()
def test_upconverter_hint_snr_high_boosts_confidence()
def test_upconverter_hint_missing_snr()
def test_upconverter_hint_different_bands()
def test_upconverter_hint_malformed_freq()
def test_upconverter_hint_single_spot()
```

### tests/test_wspr_json_output.py
```
def test_scan_json_output(monkeypatch, capsys, tmp_path)
def test_upload_json_output(monkeypatch, capsys, tmp_path)
def test_upload_json_includes_last_error(monkeypatch, capsys, tmp_path)
def test_upload_sends_heartbeat_when_requested(monkeypatch, capsys, tmp_path)
```

### tests/test_wspr_persist_creates_backup.py
```
def make_sample_config(tmp_path: Path) → Path
def test_persist_creates_backup(tmp_path: Path)
```

### tests/test_wspr_calibrate.py
```
class TestComputePpmFromOffset
  def test_positive_offset()
  def test_negative_offset()
  def test_zero_freq_raises()
class TestApplyPpmToRadio
  def test_successful_application(mock_rtlsdr_cls)
  def test_no_devices_found(mock_rtlsdr_cls)
  def test_import_error()
  def mock_import(name, *args, **kwargs)
  def test_device_error(mock_rtlsdr_cls)
class TestPersistPpmToConfig
  def test_successful_persist(mock_config)
class TestEstimateOffsetFromSpots
  def test_with_expected_freq()
  def test_without_expected_freq()
  def test_no_freqs_raises()
  def test_skips_invalid_freq()
class TestLoadSpotsFromJsonl
  def test_load_valid_file()
  def test_file_not_found()
def test_compute_ppm_from_offset_basic()
def test_estimate_offset_from_spots_with_expected()
```

### tests/test_wspr_capture.py
```
class DummyUploader
  def __init__() → None
  def enqueue_spot(spot: dict) → None
def fake_capture_fn(band_hz: int, duration_s: int)
def test_run_capture_cycle_enriches_spots(tmp_path: Path)
def test_capture_enqueues_when_metadata_present(tmp_path: Path)
def test_capture_skips_enqueue_without_grid(tmp_path: Path)
```

### tests/test_wspr_scan.py
```
def fake_capture_band1(band_hz: int, duration_s: int)
def fake_capture_band2(band_hz: int, duration_s: int)
def test_scan_bands_ranks_and_metrics()
```

### tests/test_wspr_uploader.py
```
class DummyResponse
  def __init__(status_code, text) → None
class DummySession
  def __init__(response: DummyResponse) → None
  def get(url, params, timeout)
class FakeClock
  def __init__(start: float) → None
  def advance(seconds: float) → None
def sample_spot(**overrides)
def test_enqueue_spot_creates_queue_file(tmp_path: Path)
def test_read_queue_empty(tmp_path: Path)
def test_read_queue_multiple_items(tmp_path: Path)
def test_rewrite_queue_replaces_atomically(tmp_path: Path)
def test_drain_empty_queue()
def test_drain_all_succeed(tmp_path: Path, monkeypatch)
def test_drain_partial_failure(tmp_path: Path, monkeypatch)
def test_drain_max_items_limit(tmp_path: Path, monkeypatch)
def test_drain_exception_handling(tmp_path: Path, monkeypatch)
def test_upload_spot_success_builds_params(tmp_path: Path)
def test_upload_spot_missing_metadata_skips_request(tmp_path: Path)
def test_upload_spot_http_error(tmp_path: Path)
def test_upload_spot_empty_body(tmp_path: Path)
def test_drain_records_first_failure_message(tmp_path: Path, monkeypatch)
def test_drain_daemon_backoff_skips_until_ready(tmp_path: Path, monkeypatch)
def test_drain_daemon_backoff_resets_after_success(tmp_path: Path, monkeypatch)
```

### tests/test_aprs_qconstruct.py
```
def test_append_q_construct_bytes_adds_qhop() → None
def test_append_q_construct_bytes_existing_q_remains_unchanged() → None
```

### tests/test_aprsis_client.py
```
def test_aprsis_client_connect_and_send() → None
def test_aprsis_client_login_failure() → None
def test_aprsis_client_connect_failure(monkeypatch) → None
def test_aprsis_client_connect_idempotent() → None
def test_aprsis_client_login_connection_closed() → None
def test_aprsis_client_login_response_missing() → None
def test_aprsis_client_send_without_connection() → None
def test_aprsis_client_send_binary_packet() → None
def test_aprsis_client_send_flush_error() → None
def test_aprsis_client_require_reader_without_connection() → None
def test_aprsis_client_close_ignores_errors() → None
def test_aprsis_client_logs_lifecycle(caplog) → None
def test_retry_backoff_progression() → None
def test_retry_backoff_reset_and_limits() → None
```

### tests/test_cli.py
```
def test_cli_version_flag(capsys) → None
def test_cli_aprs_listen_help(capsys) → None
def test_cli_wspr_listen_help(capsys) → None
def test_cli_aprs_diagnostics_help(capsys) → None
def test_cli_wspr_diagnostics_help(capsys) → None
def test_cli_aprs_setup_help(capsys) → None
def test_resolve_log_level_prefers_argument(monkeypatch) → None
def test_resolve_log_level_falls_back_to_env(monkeypatch) → None
def test_configure_logging_handles_oserror(monkeypatch) → None
def test_main_errors_on_unknown_args(capsys) → None
def test_main_defaults_to_listen_when_no_command() → None
def test_main_injects_listen_for_flag_only_invocation() → None
def test_setup_non_interactive(tmp_path, monkeypatch, capsys) → None
def test_setup_dry_run_interactive(tmp_path, monkeypatch, capsys) → None
def test_setup_writes_direwolf_config(tmp_path, monkeypatch) → None
def test_listen_command_once(tmp_path, monkeypatch, capsys) → None
def test_listen_reconnect_and_stats(tmp_path, monkeypatch, capsys) → None
def test_diagnostics_command_json(tmp_path, monkeypatch, capsys) → None
```

### tests/test_listen_command_extended.py
```
def test_resolve_direwolf_config_prefers_local(tmp_path) → None
def test_resolve_direwolf_config_fallback(tmp_path, monkeypatch) → None
def test_wait_for_kiss_success_after_retry(monkeypatch) → None
def test_wait_for_kiss_exhausts_attempts() → None
def test_display_frame_truncates_output(caplog) → None
def test_report_audio_error_logs_message(caplog) → None
def test_run_listen_config_missing(monkeypatch, tmp_path, caplog) → None
def test_run_listen_config_invalid(monkeypatch, tmp_path, caplog) → None
def test_run_listen_missing_direwolf_config(monkeypatch, tmp_path, caplog) → None
def test_run_listen_audio_capture_failure(monkeypatch, tmp_path, caplog) → None
def test_run_listen_direwolf_launch_failure(monkeypatch, tmp_path, caplog) → None
def test_run_listen_kiss_unreachable(monkeypatch, tmp_path, caplog) → None
def test_run_listen_receive_only_once(monkeypatch, tmp_path, caplog) → None
def test_apply_software_tocall_before_send(monkeypatch, tmp_path, caplog) → None
def test_run_listen_aprs_connect_failure(monkeypatch, tmp_path, caplog) → None
def test_run_listen_timeout_triggers_polling(monkeypatch, tmp_path, caplog) → None
def test_run_listen_kiss_client_error(monkeypatch, tmp_path, caplog) → None
def test_run_listen_skips_bad_frame(monkeypatch, tmp_path, caplog) → None
```

### tests/test_term_keyboard.py
```
def test_process_commands_dispatch_basic()
def test_start_keyboard_listener_stops_with_event()
```

### tests/test_adsb_capture.py
```
class TestAircraftState
  def test_aircraft_state_defaults()
  def test_aircraft_state_with_values()
class TestCaptureStats
  def test_capture_stats_defaults()
class TestDump1090Client
  def test_client_creation()
  def test_client_custom_path()
  def test_poll_missing_file()
  def test_poll_valid_json()
  def test_poll_invalid_json()
  def test_get_stats()
  def test_clear_stale()
  def test_get_aircraft()
class TestAdsbCapture
  def test_capture_creation()
  def test_capture_start_stop()
  def test_capture_double_start()
  def test_capture_double_stop()
  def test_capture_callback()
  def test_callback(aircraft_list)
class TestConstants
  def test_adsb_frequency()
```

### tests/test_adsb_cli.py
```
class TestAdsbCli
  def test_parser_has_adsb_mode()
  def test_adsb_listen_command()
  def test_adsb_listen_custom_json_path()
  def test_adsb_listen_custom_poll_interval()
  def test_adsb_listen_quiet_mode()
  def test_adsb_diagnostics_command()
  def test_adsb_diagnostics_json_output()
  def test_adsb_diagnostics_verbose()
class TestAdsbDiagnosticsCommand
  def test_diagnostics_command_runs(mock_run_diagnostics)
  def test_diagnostics_command_returns_error_on_failure(mock_run_diagnostics)
```

### tests/test_adsb_diagnostics.py
```
class TestDiagnosticResult
  def test_result_creation()
  def test_result_with_details()
class TestDiagnosticsReport
  def test_empty_report_is_ok()
  def test_report_with_ok_checks()
  def test_report_with_warning()
  def test_report_with_error()
  def test_report_to_dict()
class TestDump1090JsonCheck
  def test_check_missing_file()
  def test_check_valid_json()
  def test_check_invalid_json()
class TestRunDiagnostics
  def test_run_diagnostics_defaults()
  def test_run_diagnostics_with_custom_json_path()
  def test_run_diagnostics_with_adsbexchange()
```

### tests/test_adsb_reporter.py
```
class TestAdsbExchangeConfig
  def test_config_creation()
  def test_config_with_custom_input()
  def test_config_to_env_file()
  def test_config_altitude_conversion()
  def test_config_privacy_enabled()
class TestAdsbExchangeStatus
  def test_status_defaults()
class TestAdsbExchangeReporter
  def test_reporter_creation()
  def test_reporter_with_config()
  def test_reporter_custom_config_path()
  def test_is_installed_not_installed()
  def test_get_uuid_missing()
  def test_get_status()
  def test_load_config_missing_file()
  def test_load_config_valid_file()
```
